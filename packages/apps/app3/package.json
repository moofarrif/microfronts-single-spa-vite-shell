{
  "name": "@jc/app3",
  "version": "1.0.0",
  "scripts": {
    "ng": "ng",
    "start:ng": "ng serve",
    "build:ng": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "build": "ng build app3 --configuration production",
    "start": "ng s --project app3 --disable-host-check --port 4001 --live-reload false"
  },
  "private": true
}
